import { createStyles } from '@mantine/core';

export const StyledAccordion = createStyles(theme => ({
  Compact: {
    '.mantine-UnstyledButton-root': {
      padding: '5px',
      marginTop: '5px'
    }
  }
}));
